
import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    textstyles: {
        // fontsize:10,
        color: '#fff',
        
    },
    flexcontentmiddle: {
        justifyContent: 'center',
        alignItems: 'center',
        color: '#fff',
    }
  });